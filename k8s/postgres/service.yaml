apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  selector:
    app: postgres
  ports:
    - protocol: TCP
      port: 5432       # Port exposed inside the cluster
      targetPort: 5432 # Port the container is listening on
